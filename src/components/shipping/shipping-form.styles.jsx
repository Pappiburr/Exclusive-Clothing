import styled from 'styled-components';
import {BrandPrimary,BrandText} from '../../utils/colors/color.styles.jsx';

export const ShippingFormContainer = styled.div`
    display: inline-flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    width: 100%;
    height: 100%;
    background-color: ${BrandText};
    color: ${BrandPrimary};

`;


